// Custom hook for managing toast notifications
// Provides a simple interface for displaying temporary messages to users
import { useToast, toast } from "@/hooks/use-toast";

// Re-export the toast hook and function for use throughout the application
export { useToast, toast };
