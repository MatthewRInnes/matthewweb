// This file defines a reusable React component for maintaining a consistent aspect ratio,
// utilising Radix UI primitives for accessibility and custom styling.
// The component allows for displaying content in a fixed aspect ratio, ensuring that the layout remains visually balanced.

import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
